{
  "name": "securedocs",
  "version": "1.0.0",
  "description": "SecureDocs - Secure document sharing platform",
  "scripts": {
    "build": "cd admin-web && npm install && npm run build",
    "start": "node server/index.js",
    "install-server": "cd server && npm install"
  }
}
